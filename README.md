
  # Upload logo

  This is a code bundle for Upload logo. The original project is available at https://www.figma.com/design/N1IorihPYWUEQrQtsUQpdU/Upload-logo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  